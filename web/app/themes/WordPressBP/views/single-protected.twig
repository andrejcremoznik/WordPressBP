{% extends "base.twig" %}

{% block content %}
<form class="password-form" action="{{ wp_url }}/wp-login.php?action=postpass" method="POST">
  <div>
    <label class="password-form__label" for="pwbox-{{ post.ID }}">{{ i18n.password }}:</label>
    <input class="password-form__field" type="password" name="post_password" id="pwbox-{{ post.ID }}" placeholder="{{ i18n.password|e('html_attr') }}">
    <input class="password-form__submit btn" type="submit" name="Submit" value="{{ i18n.submit|e('html_attr') }}">
  </div>
</form>
{% endblock %}
